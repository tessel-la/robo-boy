# Use the complete Authorization header value expected by the private server.
ROBOBOY_PANEL_AUTHORIZATION=Bearer replace-with-a-deployment-secret

# Required by the Compose panel-management API. Use a separate long, random value.
ROBOBOY_PANEL_MANAGER_TOKEN=replace-with-a-long-random-manager-token
